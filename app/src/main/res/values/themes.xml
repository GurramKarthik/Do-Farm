<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoForm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/teal</item>
        <item name="android:statusBarColor">@color/teal</item>
        <!--
            <item name="colorPrimary">@color/coral</item>
            <item name="android:statusBarColor">@color/orange_red</item>
        -->
    </style>

    <style name="Theme.DoForm" parent="Base.Theme.DoForm" />
</resources>